// @crafted-by: Sahil Sheikh | IG: @sahil_sheikh78 | Unauthorized use prohibited
"use client"; import { useQuote } from"@/contexts/QuoteContext"; import HoverButton from "./animations/HoverButton"; interface ServiceCTAProps { onOpenQuote?: () => void; label?: string; category?: string; } export default function ServiceCTA({ onOpenQuote, label ="Get A Quote", category }: ServiceCTAProps) { const { setIsQuoteOpen, openQuoteWithCategory } = useQuote(); const handleTrigger = onOpenQuote || (() => { if (category && openQuoteWithCategory) { openQuoteWithCategory(category); } else { setIsQuoteOpen(true); } }); return ( <HoverButton onClick={handleTrigger} className="text-badge bg-white hover:bg-slate-100 text-[#0f172a] md: px-10 py-4 rounded-xl shadow-[0_10px_40px_-10px_rgba(255,255,255,0.3)]"> {label} </HoverButton> ); } 
