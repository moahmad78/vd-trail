// @crafted-by: Sahil Sheikh | IG: @sahil_sheikh78 | Unauthorized use prohibited
"use client"; import { useState, useEffect } from"react"; import { motion, AnimatePresence } from"framer-motion"; import Link from"next/link"; import { ShieldCheck, Star, Trophy, Award } from"lucide-react"; import StatsSection from"./StatsSection"; import { useQuote } from"@/contexts/QuoteContext"; const trustBadges = [ { icon: ShieldCheck, text:"Excellent BBB" }, { icon: Star, text:"Verified Ratings" }, { icon: Trophy, text:"20+ Years Legacy" }, { icon: Award, text:"Trusted Firm" } ]; const slides = [ { image:"https://images.unsplash.com/photo-1600210492486-724fe5c67fb0?q=80&w=2074", title:"ELITE RESIDENTIAL LIVING AND BESPOKE LUXURY VILLAS" }, { image:"https://images.unsplash.com/photo-1566073771259-6a8506099945?q=80&w=2070", title:"LUXURY HOSPITALITY VENUES AND PREMIUM BOUTIQUE LOUNGES" }, { image:"https://images.unsplash.com/photo-1541888086925-ebca717e8894?q=80&w=2070", title:"INSPIRING ACADEMIC SPACES AND NEXT-GEN SMART INSTITUTIONS" } ]; const Hero = () => { const [currentImage, setCurrentImage] = useState(0); const { setIsQuoteOpen } = useQuote(); useEffect(() => { const timer = setInterval(() => { setCurrentImage((prev) => (prev + 1) % slides.length); }, 6000); return () => clearInterval(timer); }, []); return ( <section className="relative w-full min-h-[90vh] flex flex-col overflow-hidden bg-[#020617]"> {/* Background Image Slider with subtle dark overlay */} <div className="absolute inset-0 z-0"><AnimatePresence><motion.div key={currentImage} initial={{ opacity: 0, scale: 1.03 }} animate={{ opacity: 0.9, scale: 1 }} exit={{ opacity: 0 }} transition={{ duration: 1.2, ease:"easeInOut" }} className="absolute inset-0"><img src={slides[currentImage].image} alt={`${slides[currentImage].title} - Voomet Design`} className="w-full h-full object-cover" /></motion.div></AnimatePresence></div> {/* Static Dark Gradient Overlay to guarantee maximum legibility of white text */} <div className="absolute inset-0 z-10 bg-gradient-to-r from-[#020617]/90 via-[#020617]/60 to-transparent" /> {/* Content Layer */} <div className="container mx-auto px-6 flex-grow relative z-20 flex items-center pt-28 md:pt-36 lg:pt-40 pb-24"><div className="max-w-3xl"><motion.div initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }} className="relative z-20"><span className="text-xs md:text-sm font-black uppercase tracking-[0.25em] text-white/80 block mb-3">We Create</span><AnimatePresence mode="wait"><motion.div key={currentImage} initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -15 }} transition={{ duration: 0.4 }} className="mb-4 sm:mb-6 min-h-[160px] md:min-h-[180px] lg:min-h-[220px]"><h1 className="leading-tight drop-shadow-sm text-3xl md:text-5xl lg:text-6xl font-black uppercase tracking-wide text-white"> {slides[currentImage].title} </h1></motion.div></AnimatePresence><p className="drop-shadow-sm max-w-xl text-slate-200 leading-relaxed font-normal text-base md:text-base"> End-to-end turnkey design and execution exclusively engineered for premium homes, luxury hotels, and modern institutions. </p> {/* Trust Badges (Desktop: static grid/flex) */} <div className="hidden md:flex flex-wrap items-center gap-6 sm:gap-10 my-8"> {trustBadges.map((badge, idx) => { const Icon = badge.icon; return ( <div key={idx} className="flex flex-col items-center gap-2"> <Icon className="w-8 h-8 sm:w-10 sm:h-10 text-white stroke-[1.5]" /> <span className="text-xs sm:text-sm uppercase font-bold tracking-wider text-white text-center whitespace-nowrap"> {badge.text} </span> </div> ); })} </div> {/* Trust Badges Marquee (Mobile: side-by-side infinite loop) */} <div className="md:hidden relative w-[100vw] -ml-6 overflow-hidden my-8 py-2 [mask-image:linear-gradient(to_right,transparent,black_5%,black_95%,transparent)]"> <motion.div className="flex w-max" animate={{ x: ["0%", "-50%"] }} transition={{ repeat: Infinity, ease: "linear", duration: 15 }} > {[0, 1].map((set) => ( <div key={set} className="flex gap-10 px-5"> {trustBadges.map((badge, idx) => { const Icon = badge.icon; return ( <div key={idx} className="flex flex-col items-center justify-center gap-2 shrink-0"> <Icon className="w-8 h-8 text-white stroke-[1.5]" /> <span className="text-[11px] uppercase font-bold tracking-wider text-white text-center whitespace-nowrap"> {badge.text} </span> </div> ); })} </div> ))} </motion.div> </div></motion.div> {/* Sleek Minimalist Navigation Gold Indicators */} <div className="flex gap-2.5 mb-10"> {slides.map((_, idx) => ( <button key={idx} onClick={() => setCurrentImage(idx)} className={`h-1.5 rounded-full transition-all duration-300 ${ currentImage === idx ?"w-10 bg-white" :"w-3 bg-white/40 hover:bg-white/70" }`} aria-label={`Go to slide ${idx + 1}`} /> ))} </div> <div className="flex flex-row gap-3 sm:gap-4 w-full sm:w-auto mt-2"> <Link href="/portfolio" className="flex-1 sm:flex-none"> <button className="w-full bg-white text-[#020617] text-[11px] sm:text-sm font-black uppercase tracking-[0.15em] sm:tracking-widest py-3.5 sm:py-3.5 px-2 sm:px-6 rounded-lg hover:bg-slate-200 transition-all duration-300 shadow-md transform sm:hover:-translate-y-1 whitespace-nowrap"> <span className="sm:hidden">Portfolio</span> <span className="hidden sm:inline">Explore Our Portfolio</span> </button> </Link> <button onClick={() => setIsQuoteOpen(true)} className="flex-1 sm:flex-none border-2 border-white text-white text-[11px] sm:text-sm font-black uppercase tracking-[0.15em] sm:tracking-widest py-3.5 sm:py-3 px-2 sm:px-6 rounded-lg hover:bg-white hover:text-[#020617] transition-all duration-300 shadow-sm transform sm:hover:-translate-y-1 whitespace-nowrap" > <span className="sm:hidden">Consultation</span> <span className="hidden sm:inline">Book a Consultation</span> </button> </div></div></div> {/* Stats Overlay */} <div className="relative z-30"><StatsSection /></div></section> ); }; export default Hero;
